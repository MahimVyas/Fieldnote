{
  "name": "fieldnote",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first evidence research workbench",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "research",
    "evidence",
    "agents",
    "local-first"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check agents.js && node --check script.js"
  }
}
